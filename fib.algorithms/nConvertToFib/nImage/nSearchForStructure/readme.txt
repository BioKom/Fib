nSearchForStructure
===================

This folder contains the namespace for function and classes that are for
searching in an image for structures (like same color circles or
rectangles) and converting them to structur objects (see nStructureData).
