


wenn DB quadrangle OK: see next two


testen list< cExtObject * > tImageStructureConvertToFib::testConvertToFib:
- test for cImageAreaSameColor:
	- + antialised
- test for cImageAreaLinearColor:
	- area: complex, search: non
	- + antialised
- test for cImageStructure:
	- create objects: dreieck, balken, kreis, rechteck teil größer als kreis
	- area: dreieck, search: non
	- area: balken, search: non
	- area: kreis, search: non
	- area: kreis, search: rechteck teil größer als kreis
	- area: quadrangle + circle, search: non
	- area: complex, search: non
	- + antialised
	
- test for cImageStructure*:
	- create objects: dreieck, balken, kreis, rechteck teil größer als kreis
	- area: dreieck, search: non
	- area: balken, search: non
	- area: kreis, search: non
	- area: kreis, search: rechteck teil größer als kreis
	- area: quadrangle + circle, search: non
	- area: complex, search: non
	- + antialised



next steps:
	- implement classes which implements iConvertToFib and inherit the structures
	-- cImageAreaLinearColor::convertToFib()
	
	- implement search algorithm for structures in image
	- ? implement method to reduce root elements




Suche von linar color Bereichen:
	- erste Ableitung konstante Bereiche suchen (fuer beide Richtungen)
		- Bereich konstant wenn (alternativen; mittelwert gut?):
			- vom ersten Punkt nur delta abweicht
			- vom Median oder Mittelwert nur delta abweicht
		-? versuche approximation über:
			- Mittelwerte erste Ableitung als Anstieg
			- Basis = Mittelwert - Anstig * Punkt


	-Vector vor linear components
		- irgendwie max und min von geraden merken? (ist das möglich?)
		- Bedingungen: größer als y_min, kleiner als y_max



- von structutur abgeleitete klasse mit methode zum erzeugen von Fib object aus struktur
	- implementiert interface nConvertToFib::iConvertToFib
		- cRoot * convertToFib() const;
- Erzeuge jeweils pro Struktur ein root mit evtl. subroots
- Methode die root objekte wieder vorteilhaft reduziert:
	- fuer jedes root objekt
		- ersetze externale Objekte durch main Fib objekt von root objekt
		- update domains
		- pruefe ob Groesse reduziert -> wenn nicht verwerfe Aenderungen
	- renummeriere die verbleibenden root objekt identifier








